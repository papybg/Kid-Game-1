# Example environment variables (DO NOT commit secrets)
# Copy to `.env` (project root) and fill with real values for local testing.

# Server (backend)
DATABASE_URL="postgres://<user>:<pass>@<host>:5432/<db>" # use test Neon DB
CLOUDINARY_URL="cloudinary://<api_key>:<api_secret>@<cloud_name>"
PORT=3005

# Client (frontend - Vite variables begin with VITE_)
VITE_API_URL="http://localhost:3005"

# Optional: other keys used by your app
# SENTRY_DSN=...
# OTHER_KEY=...
